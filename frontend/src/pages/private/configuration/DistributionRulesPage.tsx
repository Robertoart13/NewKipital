import { Typography } from 'antd';

const { Title, Paragraph } = Typography;

/**
 * Vista base para reglas de distribucion.
 * Se deja intencionalmente simple como placeholder funcional inicial.
 */
export function DistributionRulesPage() {
  return (
    <section>
      <Title level={1}>Regla de distribucion</Title>
      <Paragraph>
        Vista inicial de reglas de distribucion. Pendiente de implementar listado y detalle.
      </Paragraph>
    </section>
  );
}

